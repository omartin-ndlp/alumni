# Non-sensitive environment variables for Docker Compose
DB_NAME=site_alumni_db
DB_USER=site_alumni_user
PORT=3000

# Sensitive environment variables for Docker Compose
DB_ROOT_PASSWORD=your_mysql_root_password
DB_PASSWORD=your_app_db_password
SESSION_SECRET=a_long_random_string_for_session_secret