<div class="container-fluid">
    <h1 class="mb-4"><%= __('Liste des employeurs') %></h1>

    <% if (employers && employers.length > 0) { %>
    <div class="row">
        <% employers.forEach(employer => { %>
        <div class="col-md-6 col-lg-4 mb-4">
            <div class="card h-100">
                <div class="card-body">
                    <h5 class="card-title"><%= employer.nom %> (<%= employer.employee_count %>)</h5>
                    <p class="card-text text-muted">
                        <% if (employer.ville) { %>
                            <%= employer.ville %>
                        <% } %>
                        <% if (employer.secteur) { %>
                            <%= employer.secteur %>
                        <% } %>
                    </p>
                    <a href="/users/employers/<%= employer.id %>" class="btn btn-primary btn-sm">
                        <%= __('Voir les employés') %>
                    </a>
                </div>
            </div>
        </div>
        <% }); %>
    </div>
    <% } else { %>
    <div class="alert alert-info" role="alert">
        <%= __('Aucun employeur trouvé.') %>
    </div>
    <% } %>
</div>